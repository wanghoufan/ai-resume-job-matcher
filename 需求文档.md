# AI 求职助手产品需求文档

## 1. 产品概述

AI 求职助手帮助求职者根据目标岗位，快速分析已有简历并优化求职材料。用户上传简历 PDF、粘贴岗位描述后，系统提取简历文本并调用 AI 模型，输出岗位匹配、简历优化、求职信、面试题及能力可视化结果。

### 1.1 目标用户

- 正在准备求职材料的应届生、实习生、职场新人及有经验求职者。
- 希望针对具体职位快速优化简历与求职沟通材料的用户。

### 1.2 第一版目标

- 让用户在一个页面完成材料输入、AI 生成和结果回顾。
- 支持内置 DeepSeek 与自定义 OpenAI Chat Completions 兼容模型服务。
- 在不引入数据库的前提下，使用当前页面状态保留本次会话的生成历史。

### 1.3 范围边界

**包含**：PDF 简历解析、岗位匹配分析、简历优化建议、求职信、面试题、能力雷达图、当前页面历史与多模型配置。

**不包含**：用户注册/登录、数据库、跨设备同步、刷新后历史恢复、简历编辑回写、模型密钥持久化。

## 2. 用户流程

1. 用户打开网站，上传本人简历 PDF。
2. 浏览器端解析 PDF 中的文字，向用户显示解析成功状态。
3. 用户粘贴目标岗位描述（JD）。
4. 用户选择模型来源：默认 DeepSeek，或自定义兼容服务。
5. 用户点击“开始分析”。
6. 页面立即进入加载状态，显示“AI 正在研究中…”及轮换的鼓励语。
7. 服务端代理调用选定模型，将结构化结果返回前端。
8. 页面展示分析结果与能力雷达图，同时将该次结果写入当前页面的历史记录。
9. 用户可从历史记录中打开任意一次生成结果再次查看。

## 3. 功能需求

### 3.1 简历 PDF 上传与解析

- 仅接受 `.pdf` 文件；需提示不支持的格式。
- 前端使用合适的 PDF 文本提取库解析文本（建议使用 `pdfjs-dist`）。
- 解析完成后需显示文件名和成功状态。如果 PDF 为扫描件或无可提取文本，应提示用户上传包含可选中文字的 PDF。
- 发给 AI 的仅为解析后的简历文本，不传输 PDF 原文件。

### 3.2 岗位描述输入

- 提供多行文本输入区，用于粘贴完整 JD。
- JD 为必填项；为空时不允许提交，并显示明确校验提示。

### 3.3 模型选择与配置

#### 内置 DeepSeek（默认）

- 页面默认使用 DeepSeek，模型配置区默认折叠。
- 密钥仅从服务端环境变量 `DEEPSEEK_API_KEY` 读取，不得写入前端代码、不得暴露至浏览器。
- 请求地址和默认模型名在服务端预设。

#### 自定义模型服务

- 用户切换至“自定义模型服务”后，显示以下必填字段：API 请求地址、模型名称、API Key。
- 支持 OpenAI Chat Completions 协议兼容的模型厂商。
- 发送前需校验请求地址为有效 HTTP/HTTPS URL，并校验三个字段均非空。
- 自定义 API Key 仅用于当次请求，不写入环境变量、不持久化、不写入历史记录、不写入日志。刷新页面后清空。

### 3.4 AI 分析结果

AI 必须返回可解析的结构化 JSON，包含以下字段：

```ts
type AnalysisResult = {
  matchScore: number; // 0–100
  matchAnalysis: string;
  optimizationSuggestions: string[];
  coverLetter: string;
  interviewQuestions: Array<{
    question: string;
    referenceAnswer: string;
  }>;
  radarSkills: Array<{
    name: string;
    score: number; // 0–100
  }>;
};
```

页面必须展示：

1. **岗位匹配度分析**：总分及对岗位要求、简历优势、差距的文字分析。
2. **简历优化建议**：以可执行的清单呈现。
3. **定制求职信**：提供可直接复用的求职信正文。
4. **面试预测**：恰好 10 个问题，每个都配套参考回答。
5. **能力雷达分布图**：将简历能力可视化。默认维度为专业技能、项目经验、沟通协作、行业理解、问题解决、岗位契合度，每个维度为 0–100 分。

### 3.5 历史记录

- 每次成功生成后，将该次结果写入 React 页面状态。
- 单条历史记录包含生成时间、岗位描述摘要、模型来源标识与完整分析结果。
- 用户可点击历史项查看其对应结果。
- 不使用数据库、`localStorage`、Cookie 或其他持久化手段；页面刷新后历史自然清空。
- 历史记录绝不包含自定义 API Key。

## 4. 交互与视觉设计

### 4.1 界面布局

- **品牌区**：产品名称、一句价值主张与简洁的科技感装饰。
- **材料输入区**：简历上传、JD 文本区、模型选择及配置、生成按钮。
- **生成状态区**：加载动画、“AI 正在研究中…”、鼓励语和进行状态。
- **结果区**：分数概览、雷达图与四类文本分析卡片。
- **历史记录区**：当前页面生成记录列表与查看入口。

### 4.2 视觉风格

- 整体为深蓝科技风：以深蓝色背景和深海蓝卡片建立层次。
- 以明亮蓝色和蓝紫渐变用于主按钮、重点数据、进度和图表高亮。
- 使用现代无衔线字体，按卡片、标题、正文、数字指标建立清晰的信息层级。
- 图表和文字必须保持充足对比度，支持桌面端与移动端阅读。
- 上传、按钮反馈、加载状态和结果出现使用轻量动效，避免干扰阅读。

### 4.3 生成中体验

- 点击“开始分析”后立即显示加载状态并禁用再次提交。
- 固定显示“AI 正在研究中…”，并轮换显示鼓励文案，包括：“你的未来不是梦”、“好岗位正在为你准备”、“每一次准备，都离理想更近一步”。
- 成功后转入结果区；失败后显示原因概要和“重试”入口。

## 5. 技术设计

### 5.1 应用架构

- 使用 Next.js 构建前端页面。
- PDF 文本在浏览器端解析。
- Next.js API Route 负责统一校验请求、构建提示词、调用模型服务、解析模型结果和安全返回错误。
- API Route 不记录包含 API Key 的请求体、请求头或错误对象。

### 5.2 模型调用策略

- DeepSeek 模式使用服务端的 `DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL` 和 `DEEPSEEK_API_KEY`；应用在基地址后追加 `/chat/completions`。
- 自定义模式将用户填写的地址、模型和 API Key 仅用于当次服务端转发。
- 服务端为上游请求设置超时控制，并对上游错误信息进行脱敏后再返回。
- 要求模型严格按约定 JSON 结构返回；解析失败时应向用户提示“模型返回结果格式异常，请重试或更换模型”。

## 6. 异常处理

| 场景 | 页面处理 |
| --- | --- |
| 未上传 PDF | 提示“请上传简历 PDF”并阻止提交。 |
| 非 PDF 文件 | 提示只支持 PDF 格式。 |
| PDF 无可提取文本 | 引导用户上传可选中文字的 PDF。 |
| JD 为空 | 提示“请粘贴目标岗位描述”并阻止提交。 |
| 自定义配置不完整 | 提示缺少的请求地址、模型名或 API Key。 |
| 自定义地址无效 | 提示必须填写合法 HTTP/HTTPS URL。 |
| API 请求失败或超时 | 显示脱敏错误说明和重试入口。 |
| 模型返回非结构化结果 | 提示更换模型或重试。 |

## 7. 验收标准

1. 用户上传可解析 PDF 并填写 JD 后，可成功获得并展示岗位匹配、简历建议、求职信、10 个面试问答和能力雷达图。
2. 配置 `DEEPSEEK_API_KEY` 后，默认 DeepSeek 流程可正常工作，密钥不出现在前端代码或浏览器网络响应中。
3. 填写有效的 OpenAI Chat Completions 兼容地址、模型名和 API Key 后，自定义模型流程可完成同样的分析。
4. 自定义凭据不存入历史记录、本地存储、Cookie 或日志，刷新后自定义配置与历史均清空。
5. 加载期间立即显示“AI 正在研究中…”和鼓励文案，且不允许重复提交。
6. 当前会话中每次成功生成都可从历史记录查看；页面刷新后记录清空。
7. 界面在桌面端与移动端均保持清晰、可读，并符合深蓝科技风视觉要求。

## 8. 生产化：账号、历史与数据留存

### 8.1 账号与访问

- 用户需要通过 Supabase Auth 使用永久账号登录，推荐首版提供邮箱验证码/魔法链接登录，并预留 Google、GitHub 登录扩展能力。
- 仅已登录用户可以发起正式分析、保存简历、查看历史和使用套餐额度；未登录用户可浏览产品介绍与登录入口。
- 用户再次登录后，应恢复其简历、岗位描述、历史分析、套餐与用量信息。
- 所有业务数据均以 `auth.users.id` 作为所有者；客户端不得自行指定或修改 `user_id`。

### 8.2 历史保存与对比

- 每次点击分析时先创建一条 `processing` 状态的分析记录；模型成功后写入完整结果、匹配分和完成时间，失败时保留失败状态与不含密钥的错误摘要。
- 简历、岗位描述和分析结果均按“不可变快照”处理。用户修改或再次上传材料时创建新记录，不覆盖已被历史分析引用的内容。
- 历史页支持按简历、岗位、时间、模型与分析状态筛选；用户可选择两条已完成记录比较匹配分、雷达维度和优化建议。
- 用户可下载或删除自己的简历及分析记录；删除简历时同步删除私有存储中的原 PDF，删除分析时同步删除其结果和关联用量记录。

### 8.3 隐私与留存

- 简历 PDF 存放在私有 Supabase Storage bucket `resumes`，路径统一为 `{user_id}/{resume_id}/original.pdf`；禁止公开 bucket。
- 简历原文、岗位描述、AI 结果及 API 用量均受 RLS 保护，用户只能访问自己的记录。
- 不保存用户自行填写的第三方模型 API Key；服务端模型密钥仅保留在环境变量或受限的密钥管理中。
- 用户可在账户设置中发起数据导出和永久删除请求；删除后不再保留可识别的简历与岗位内容。

## 9. 数据库设计

### 9.1 已有核心表

| 表 | 用途 | 说明 |
| --- | --- | --- |
| `profiles` | 用户资料 | 以 `auth.users` 为主键扩展展示名、头像和语言设置。 |
| `resumes` | 简历快照 | 保存文件路径、文件元数据、解析文本和当前使用标记。 |
| `job_descriptions` | 岗位描述快照 | 保存职位名称、公司、地点、来源链接和原始内容。 |
| `analyses` | AI 分析主记录 | 关联简历与岗位，保存状态、匹配分、模型、提示词版本和结果 JSON。 |
| `subscriptions` | 用户当前订阅 | 保存用户当前套餐、周期和支付服务商订阅标识。 |
| `usage_events` | 不可变用量事件 | 每次调用记录输入/输出 token、估算成本、状态及关联分析。 |

### 9.2 建议新增表

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `plans` | `code`、`name`、`monthly_price_cents`、`currency`、`analysis_limit`、`token_limit`、`is_active` | 套餐目录；免费版、专业版和企业版均由数据配置，不写死在代码中。 |
| `usage_periods` | `user_id`、`subscription_id`、`period_start`、`period_end`、`analysis_limit`、`analysis_used`、`token_limit`、`tokens_used` | 每个账单周期的额度快照与汇总，用于分析前的额度校验和用户用量展示。 |
| `billing_events` | `provider`、`provider_event_id`、`user_id`、`subscription_id`、`event_type`、`amount_cents`、`currency`、`status`、`payload`、`occurred_at` | 支付平台 Webhook 的幂等账务流水；仅服务端可写，支持未来接入 Stripe、支付宝或微信支付。 |

### 9.3 建议补充字段与索引

- `analyses` 增加 `result_schema_version`、`provider_request_id`、`latency_ms`，便于追踪 AI 返回结构、供应商请求和性能。
- `resumes` 与 `job_descriptions` 增加 `content_hash`，用于识别重复材料并保证快照可追溯。
- 为 `analyses.resume_id`、`analyses.job_description_id` 建立索引；现有数据库性能检查已提示这两个外键缺少覆盖索引。
- 保留现有 `usage_events` 作为唯一成本事实来源；不要用页面计数替代 token/成本事件。

### 9.4 用量与计费规则

1. 服务端在调用 AI 前，根据当前 `usage_periods` 锁定并检查用户是否仍有分析额度。
2. 每次请求都创建 `usage_events`，无论成功或失败均记录实际 token、模型和内部成本；失败请求默认不扣除用户分析次数，但用于成本分析。
3. 成功分析后原子地更新周期汇总；不可由浏览器直接写入用量、套餐状态或支付记录。
4. 套餐价格、额度与模型成本必须数据化并保留历史快照，避免未来调价后影响已发生的账务统计。

### 9.5 权限规则

- `profiles`、`resumes`、`job_descriptions`、`analyses`、`subscriptions`、`usage_events`、`usage_periods`：启用 RLS，且读写范围均限制为 `(select auth.uid()) = user_id`。
- `plans`：已登录用户只读；套餐维护仅允许受信任的后台服务。
- `billing_events`：不向浏览器授予直接读写权限；支付 Webhook 通过服务端安全验证后写入。
- Storage：只允许认证用户在自己的 `{user_id}/` 目录上传、读取和删除 PDF；限制 MIME 类型为 PDF、文件大小为 10 MB。
